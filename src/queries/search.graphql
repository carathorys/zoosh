query search($query: String!) {
    searchMovies(query: $query) {
        id
        name
        adult
        overview
        status
        popularity
        releaseDate
        backdrop {
            small,
            medium,
            large,
            original
        }
        poster {
            tiny
        }
    }
}
